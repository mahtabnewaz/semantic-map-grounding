# Onboarding study (Phase 6)

Measures human correction effort on agent-generated capability
descriptors vs. authoring them from scratch (CLAUDE.md, Deploy claim).

- Inputs: platform artefacts under `../../data/platforms/`
- Draft generation: `src/onboarding.py`
- Validation: `src/descriptor.py` (non-model checker)
- No generated descriptor is used unreviewed (CLAUDE.md #8).

Timestamped CSVs land here or in `../results/`.
